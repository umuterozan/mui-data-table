import { Container } from "@mui/material";
import { Head, Body, DataTable } from "./components";

function App() {
    return (
        <Container maxWidth="xl">
            <Head />
            <Body />
            <DataTable />
        </Container>
    );
}

export default App;
